package ParkingLot;

import ParkingSpot.ParkingSpotManager;
import Vehicle.VehicleType;

public class ParkingManagerFactory {

    public ParkingSpotManager getManager(VehicleType vehicleType, StrategyType strategyType) {
        // todo can it be done without StrategyType

    }
}
